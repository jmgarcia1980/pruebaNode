# NOTAS:

detalle de nortas
```
Options:
      --help     Show help                                             [boolean]
      --version  Show version number                                   [boolean]
  -b, --base     Base usada para generar el archivo de multiplicar
                                                             [number] [required]
  -l, --listar   Valor que permite listar el archivo de multiplicar en pantalla
                                                      [boolean] [default: false]
  -h, --hasta    Valor hasta para generar                 [number] [default: 10]
```
